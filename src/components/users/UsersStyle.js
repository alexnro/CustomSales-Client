import { makeStyles } from '@material-ui/core/styles';


const useStyles = makeStyles((theme) => ({
    root: {
        marginTop: "6%"
    },
    modalDivider: {
        marginBottom: "5%"
    },
    addButton: {
        marginBottom: "2%",
        marginLeft: "3%"
    }
}));

export { useStyles };