import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles(() => ({
    modal: {
        color: "red"
    },
    name: {
        marginRight: "50px"
    },
    quantity: {
        marginRight: "50px"
    },
    price: {
        marginRight: "50px"
    },
    addButton: {
        marginRight: "5%",
    }
}))

export { useStyles };
